# Magic Survival Companion Seed Data Notes

## 목적

이 데이터는 **Magic Survival v0.992 조합 보조 웹앱을 바로 개발하기 위한 seed dataset**입니다.

현재 포함 범위:

- 액티브 마법 21종
- 액티브 마법의 특성 단계/특성
- 조합과 직접 관련된 일반 패시브 3종
  - 지능
  - 마력 발산
  - 집중
- 마법 조합 63종
- 조합 슬롯/마법 잠금/특수 조합 규칙

궁극기, 전체 아티팩트, 클래스/실험체, 티어 정보는 MVP에서 제외했습니다.

## 데이터 구조 원칙

UI 코드에 마법명이나 조합 조건을 하드코딩하지 않습니다.

- `magics.json`: 액티브 마법과 특성
- `passives.json`: 조합에 필요한 패시브
- `combinations.json`: 조합 요구조건 및 특수 효과
- `rules.json`: 런 단위의 공통 조합 규칙
- `metadata.json`: 버전/출처/검증 상태
- `magic-survival-v0.992.bundle.json`: 위 데이터를 하나로 합친 편의용 파일

향후 게임 업데이트 시 **JSON만 수정해도 앱이 자동으로 새 데이터를 반영**하도록 구현하는 것이 목표입니다.

## 특히 중요한 예외

### 마력탄

마력탄은 Lv.4와 Lv.7에 각각 별도 특성 선택이 있습니다.

따라서 조합 엔진은 단순히 `selectedTrait` 하나만 저장하면 안 되고,
`traitStage -> selectedTraitId` 형태로 관리해야 합니다.

예:

```ts
traits: {
  4: "fireworks",
  7: "doppelganger"
}
```

`불꽃놀이` 조합은 Lv.4의 `폭죽`만 요구하므로 Lv.7 특성은 무엇을 골라도 조건을 깨지 않습니다.

### 완료된 조합에 사용한 액티브 마법

한 번 조합에 사용된 액티브 마법은 주력/재료 여부와 관계없이
다른 조합 계획에서는 더 이상 사용 가능한 재료로 계산하지 않는 방향으로 MVP 엔진을 설계합니다.

이 규칙 때문에 조합 후보는 매 선택/조합 완료 후 다시 계산해야 합니다.

### 양자폭발

게임 화면 직접 확인:

- 보호막(파괴장)
- 마력 발산

웹의 오래된 요약표에서 두 번째 조건이 생략된 경우가 있으므로
현재 seed에서는 게임 화면 확인 결과를 우선했습니다.

### 텔레포트

게임 화면 직접 확인:

- 유체화(공간 왜곡)
- 집중

### 오버마인드

- 지능 Lv.5 MAX
- 마법진(대마법진)

일반 조합과 달리 빌드 전체 동작에 영향을 주는 특수 조합입니다.

### 무한동력

- 마법진(마력 해방)
- 지능 Lv.5 MAX

### 창조의 문

- 지능 Lv.5 MAX
- 마법진(물질 연성)
- 조합 가능 횟수 +2
- 최대 레벨 +5

### 데우스 엑스 마키나

- 오버마인드 완료 필요
- 3번째 조합에서만 가능
- 완료 후 추가 조합 불가

## 검증 상태

`combinations.json`의 각 조합에는:

```json
"verification": {
  "status": "...",
  "gameVersion": "0.992"
}
```

가 들어 있습니다.

현재:

- `user_verified_game_screenshot`
  - 양자폭발
  - 텔레포트
- 나머지
  - `web_seed_unverified_in_game`

실제 게임 도감과 비교한 뒤 하나씩:

```json
"status": "verified_in_game"
```

로 바꾸면 됩니다.

## 출처

데이터는 2026년 7~8월 최신 상태로 갱신된 한국어/일본어 커뮤니티 자료를 seed로 정규화했습니다.
원문 설명을 그대로 복제하는 것이 아니라, 앱에 필요한 이름/관계/짧은 효과 요약을 구조화한 데이터입니다.

최종적으로는 **게임 내 도감을 가장 높은 신뢰도**로 두고 검수하는 것을 권장합니다.
