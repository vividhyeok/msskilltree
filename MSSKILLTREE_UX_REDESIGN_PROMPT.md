# Magic Survival Companion — 게임 이해 기반 UX 전면 개편 프롬프트

대상 저장소:

```text
https://github.com/vividhyeok/msskilltree
```

현재 배포:

```text
https://msskilltree.vercel.app/
```

---

# 0. 이번 작업의 성격

현재 앱은 데이터 구조, 조합 엔진, 충돌 판정, localStorage, Undo 등 **기능적 기반은 이미 충분히 좋다.**

문제는 UX다.

현재 UI는 기능을 많이 보여주는 **관리 대시보드 / 공략 사이트**에 가깝다.

하지만 실제 제품은 다음 상황에서 사용된다.

> 스마트폰으로 Magic Survival 플레이  
> → 레벨업 화면에서 게임을 잠깐 멈춤  
> → 옆의 태블릿에서 필요한 정보를 1~3번 터치 또는 한 번 훑어봄  
> → 어떤 마법/특성을 가져갈지 확인  
> → 다시 게임으로 돌아감

따라서 이번 작업은 새로운 기능을 많이 넣는 작업이 아니다.

**Magic Survival이라는 게임이 실제로 어떻게 선택을 요구하는지 이해한 뒤, 그 선택 흐름에 맞게 UI를 다시 만드는 작업**이다.

이 문서에 정리된 게임 플레이 맥락을 제품 요구사항으로 취급하라.

웹 검색이 가능하더라도 이번 작업의 핵심 설계를 다시 임의로 재해석하지 말고,
아래의 플레이 흐름과 UX 원칙을 기준으로 수정한다.

---

# 1. 제품의 한 문장 정의

> **전략은 플레이어가 선택하고, 암기와 조합 가능성 계산은 앱이 대신한다.**

이 앱이 해결해야 하는 실제 문제:

```text
"화염구랑 합치는 게 전기충격이었나 낙뢰였나?"

"이 마법을 최종적으로 어느 특성으로 전직해야 했지?"

"지금 뜬 3개 중에서 내가 목표로 한 조합에 필요한 게 있나?"

"아까 이 마법은 다른 조합에 써버렸는데 이 조합도 아직 가능한가?"

"내가 A 조합 때문에 이 스킬을 올리던 거였나 B 때문이었나?"
```

이런 순간에 사용자가 Wiki를 검색하거나 조합표를 읽지 않도록 하는 것이 목적이다.

---

# 2. 먼저 이해해야 할 실제 Magic Survival 플레이 흐름

아래는 현재 v0.992 기준으로 조사한 게임의 핵심 선택 구조다.

## 2.1 전투 자체는 자동 공격 중심

Magic Survival은 캐릭터 이동이 직접 조작의 중심이고,
습득한 공격 마법은 기본적으로 자동으로 발동한다.

즉 게임 도중 플레이어의 가장 큰 전략적 개입 중 하나는
**레벨업 때 어떤 마법을 선택해서 빌드를 만들 것인가**다.

### UX 의미

이 Companion은 전투 피지컬을 돕는 도구가 아니다.

핵심은:

```text
레벨업 선택
→ 성장 경로
→ 올바른 특성
→ 조합 완성
```

을 빠르게 기억시켜주는 것이다.

---

# 2.2 레벨이 오를 때마다 랜덤한 마법 선택지가 나온다

현재 게임에서는 경험치 게이지가 차서 레벨이 오를 때
일반적으로 **랜덤한 마법 3개가 선택지로 제시**된다.

이미 한 번 습득한 마법은 다시 선택지에 등장할 가능성이 더 높다.

일부 Artifact, Class, Research 등의 효과로
선택지가 4개가 되는 경우도 있다.

따라서 앱에서 "게임은 항상 정확히 3개를 보여준다"고 하드코딩하지 마라.

### UX 의미

플레이어가 실제로 보는 상황은:

```text
[화염구] [낙뢰] [집중]
```

같은 **소수의 후보 중 하나를 순간적으로 골라야 하는 상황**이다.

그러므로 Companion의 핵심은
63개 조합 전체를 잘 정리해서 보여주는 것이 아니다.

플레이어가 게임 화면의 3~4개 후보를 보고 Companion을 훑었을 때:

```text
"아, 화염구는 내가 지금 필요함."
```

을 즉시 알아야 한다.

따라서 **현재 목표에 필요한 마법 목록(shopping list)** 이
매우 높은 시각적 우선순위를 가져야 한다.

---

# 2.3 Mana Retrieval은 선택을 미루고 다시 뽑는 기능이다

레벨업 화면에는 마법을 즉시 선택하지 않고
선택지를 다시 뽑는 Mana Retrieval 계열 동작이 존재한다.

즉 플레이어는 단순히 "뜬 것 중 아무거나 선택"하는 것이 아니라,
목표 빌드를 위해 필요한 마법이 안 뜨면 선택을 미룰 수 있다.

### UX 의미

Companion이 해야 할 일은:

```text
"지금 무조건 X를 고르세요"
```

가 아니다.

대신:

```text
현재 목표에서 필요한 것:
에너지탄 → 플레어
눈보라 → 별똥별
위성 → 핵융합
```

처럼 **무엇을 기다리고 있는지** 즉시 알려줘야 한다.

이 정보가 있어야 플레이어가 게임의 랜덤 선택과 리롤을 전략적으로 활용할 수 있다.

---

# 2.4 마법은 성장 끝에서 특성을 선택한다

전투 마법은 최대 레벨에서 보통 3개의 파생 특성 중 하나를 고른다.

조합 마법은 단순히:

```text
화염구 + 에너지탄
```

이 아니라 대부분:

```text
화염구 / 대폭발
+
에너지탄 / 플레어
```

처럼 **정확한 특성 경로까지 요구한다.**

마력탄은 특히 예외적이다.

현재 seed 데이터처럼
마력탄은 Lv.4와 Lv.7에 서로 다른 특성 선택 단계가 있으며,
어떤 조합은 Lv.4 특성을 검사하고,
다른 조합은 Lv.7 특성을 검사한다.

### UX 의미

사용자가 기억해야 하는 것은 단순한 마법 이름이 아니다.

반드시 한 세트로 보여줘야 한다.

```text
에너지탄
→ 플레어
```

따라서 목표 재료를 보여줄 때:

나쁜 예:

```text
에너지탄 필요
```

좋은 예:

```text
에너지탄 4/7
→ 플레어
```

---

# 2.5 조합 마법은 레벨 25 / 50 / 75에 슬롯이 열린다

기본적으로 캐릭터 레벨이:

```text
25
50
75
```

에 도달할 때마다 마법 조합 가능 횟수가 1회씩 증가한다.

따라서 일반적인 Run에서는 기본 3회의 조합이 가능하다.

조합은 일반 랜덤 마법 3택 중 하나로 등장하는 것이 아니라,
**레벨업 화면의 별도 조합 버튼을 통해 실행하는 구조**다.

특수 조합이나 Artifact에 의해 조합 가능 횟수가 추가될 수 있다.

### UX 의미

Companion은 매 레벨마다 플레이어 레벨까지 입력시키면 안 된다.

그건 실제 게임보다 Companion 관리가 더 번거로워지는 역전 현상이다.

기본적으로:

```text
조합 완료 0 / 3
```

정도만 보여주고,
사용자가 실제 게임에서 조합 가능한 타이밍에
`완료`를 누르는 방식이면 충분하다.

특수 보너스 슬롯은 데이터 rule로 처리한다.

---

# 2.6 조합은 "강화 주체"와 "재료"가 다르다

일반 조합은 두 마법이 역할이 동일하지 않다.

하나는 조합 후 강화되는 주체이고,
다른 하나는 재료가 된다.

재료로 사용된 마법은 조합 후 더 이상 일반 마법으로 사용되지 않으며,
다른 조합의 재료로 재사용할 수 없다.

또한 조합 이후에는 기본적으로 강화 주체 쪽과 관련된 효과가 중요해지고,
재료 마법 전용 강화는 가치가 사라질 수 있다.

### UX 의미

이것은 현재 앱의 constraint engine이 반드시 계속 처리해야 한다.

예:

```text
A 데몬 방정식 완료

화염구 → A에 사용됨
에너지탄 → A에 사용됨
```

이 순간 에너지탄을 요구하는 다른 조합은
기본 후보에서 즉시 사라져야 한다.

사용자가:

```text
"분명 이 조합도 에너지탄 쓰는데 왜 안 되지?"
```

라고 다시 계산하게 만들면 안 된다.

기본 화면에서는 **현재 Run에서 실제 도달 가능한 조합만** 보여라.

불가능한 경로는 별도 보조 보기로 내린다.

---

# 2.7 조합에는 올바른 특성이 필수다

두 마법을 MAX까지 올렸더라도
필요한 특성을 잘못 선택하면 해당 조합은 만들 수 없다.

예를 들어 목표가:

```text
화염구 / 대폭발
+
에너지탄 / 플레어
→ 데몬 방정식
```

인데 화염구에서 독가스를 선택하면
데몬 방정식 경로가 막힌다.

### UX 의미

현재 구현의 **목표 조합과 충돌하는 특성 선택 경고**는 좋은 기능이다.

반드시 유지한다.

다만 경고문은 짧게 한다.

```text
A 데몬 방정식이 막힙니다.

필요: 대폭발
선택: 독가스

[취소] [그래도 선택]
```

---

# 2.8 Artifact 선택은 레벨업 마법 선택과 다른 흐름이다

Artifact는 필드의 보물상자, 웨이브 보상, 상인 등의 별도 흐름으로 얻는다.

일반 보물상자는 보통 랜덤 Artifact 3개 중 하나를 선택한다.

Artifact는 마법 성능과 조합 빌드에 영향을 주지만,
**지금 만들고 있는 MVP의 가장 큰 문제는 마법/특성/조합 암기다.**

### UX 의미

Artifact DB를 기본 Run 화면에 추가하지 마라.

그것까지 동시에 보여주면 다시 정보 대시보드가 된다.

현재 단계에서는:

```text
마법 선택
특성
조합
조합 슬롯
```

에만 집중한다.

Artifact는 향후 별도 기능으로 확장할 수 있다.

현재 조합 슬롯에 직접 영향을 주는
`권능의 영역` 같은 예외만 existing rule을 유지하면 충분하다.

---

# 2.9 Ultimate Magic은 Lv.100의 별도 시스템이다

현재 게임에는 특정 Class + Subject 조건을 만족하고
Lv.100에 도달했을 때 사용하는 Ultimate Magic 시스템도 있다.

Lv.100은 일반 마법 선택 대신 Ultimate 습득에 사용된다.

하지만 이것은 현재 사용자가 가장 불편해하는
**일반 마법 조합 암기 문제와는 별도의 후반 시스템**이다.

### UX 의미

이번 UX 전면 개편에서는 Ultimate를 기본 화면에 억지로 추가하지 마라.

현재 MVP의 핵심 흐름을 먼저 완성한다.

데이터/엔진은 미래 확장 가능하도록 유지하면 된다.

---

# 3. 이 게임 구조로부터 도출되는 핵심 UX 원칙

## 원칙 A — "전체 정보"보다 "지금 필요한 것"

플레이어는 Wiki를 공부하는 중이 아니다.

게임 화면에 랜덤 선택지가 떴을 때:

```text
이 중 내 빌드에 필요한 게 뭐지?
```

를 확인하는 중이다.

따라서 기본 UI 우선순위는:

```text
1. 내가 목표로 잡은 조합
2. 그 목표에 아직 필요한 마법 + 필요한 특성
3. 지금 내가 가진 마법의 현재 레벨
4. 선택한 마법이 열어주는 다른 조합
5. 전체 조합 catalog
```

다.

현재 앱은 4~5번의 비중이 너무 크다.

---

# 원칙 B — 고정 위치가 검색보다 빠르다

현재 앱의 필터:

```text
전체
보유
목표
미보유
```

는 누를 때마다 카드의 위치를 재배열한다.

게임 중 2~5초 사용하는 도구에서는 좋지 않다.

모든 액티브 마법은 항상 **같은 위치**에 있어야 한다.

플레이를 반복하면 사용자가:

```text
화염구는 왼쪽 위
낙뢰는 그 옆
에너지탄은 여기
```

처럼 공간 위치를 기억하게 된다.

이 muscle memory가 검색창보다 빠르다.

따라서 기본 Run 화면에서 필터와 검색을 제거하고,
마법 Grid의 순서를 절대 동적으로 바꾸지 마라.

---

# 원칙 C — 게임에서 선택한 것을 기록하는 비용이 작아야 한다

Companion 때문에 게임보다 더 많은 조작을 하면 실패다.

현재 앱처럼 마법 하나를 기록하기 위해
큰 카드와 여러 UI를 읽게 하지 않는다.

레벨 기록은 한 번의 큰 `+` 터치면 된다.

---

# 원칙 D — "살펴보기"와 "기록"을 혼동하지 않는다

현재 구현은 마법 카드 전체를 탭하면:

```text
레벨 +1
+
해당 마법을 선택
```

이 동시에 일어난다.

하지만 실제 사용에서는 게임에서 선택하기 **전에**
이 마법이 어디로 이어지는지 잠깐 확인하고 싶을 수 있다.

그러므로 새 UI에서는 역할을 분리하는 것을 권장한다.

예:

```text
┌───────────────┐
│ 화염구      A │  ← 카드 본체 탭: 오른쪽에서 경로만 확인
│ Lv.4/7    [+] │  ← + 영역 탭: 실제 게임에서 골랐다고 기록
└───────────────┘
```

`+`를 누르면 자동으로 해당 마법을 focus해도 된다.

이 구조는 잘못된 상태 입력을 줄인다.

반드시 한 카드 안에서 빠르게 동작해야 하며,
`+`는 최소 44px 터치 영역을 확보한다.

---

# 원칙 E — 목표가 생긴 뒤에는 Shopping List가 가장 중요하다

예:

```text
TARGET A · 데몬 방정식
화염구  5/7 → 대폭발
에너지탄 2/7 → 플레어

TARGET B · 번개 작렬
낙뢰    4/7 → 청천벽력
전기충격 1/7 → 연쇄 번개
```

여기서 현재 게임 선택지가:

```text
에너지탄 / 서릿발 / 집중
```

이면 사용자는 Companion에서 생각할 필요 없이:

```text
에너지탄 = A
```

를 바로 인지할 수 있어야 한다.

따라서 `현재 목표에 필요한 것`은
항상 고정된 위치에 보여라.

---

# 4. 현재 코드에서 확인된 UX 문제

현재 repo의 주요 UI는 `src/main.tsx`, `src/style.css`에 집중되어 있다.

현재 디자인에서 특히 바꿔야 할 것:

## 4.1 헤더가 너무 크다

현재 CSS의 header height:

```text
84px
```

게임 Companion에는 과하다.

대략:

```text
48~54px
```

로 줄여라.

---

# 4.2 마법 카드가 너무 크다

현재 `.magic-main`:

```text
min-height: 155px
```

수준이다.

1024x768에서 기본 3열이 되므로
21개 액티브 마법을 보려면 여러 줄 스크롤이 필요하다.

이건 가장 큰 UX 실패다.

마법 Tile은 대략:

```text
56~68px
```

정도로 줄여라.

---

# 4.3 기본 Grid는 모든 21개 마법을 한 화면에 보여라

Tablet landscape 1024x768을 기준으로
모든 액티브 마법 21개를 **스크롤 없이** 보여야 한다.

추천 시작점:

```text
7 columns × 3 rows
```

왼쪽 영역이 약 600px라면
각 tile을 약 75~82px 폭으로 잡을 수 있다.

실제 한글 마법 이름을 넣어보고
6열/7열 중 더 좋은 쪽을 선택하라.

중요한 것은:

```text
스크롤 없이 21개
+
각 tile 최소 44px touch target
```

이다.

---

# 4.4 decorative 정보가 너무 많다

현재 카드에는:

- sigil
- progress bar
- 큰 빈 공간
- trait preview
- 별도 특성 버튼

등이 있다.

기본 Tile에서 제거/축소한다.

필수만 남긴다.

```text
화염구   A
4/7    [+]
```

MAX + 특성 선택 후:

```text
화염구   A
MAX · 대폭발
```

정도면 충분하다.

---

# 4.5 검색/필터가 기본 UI 공간을 차지한다

현재의:

```text
전체 | 보유 | 목표 | 미보유
마법 검색
```

은 기본 Run 화면에서 제거한다.

필요하면 `⋯` 메뉴 또는 별도 Explore/Catalog 화면으로 옮긴다.

---

# 4.6 오른쪽 패널이 공략 카드 모음처럼 보인다

현재 조합 하나마다:

- 카드
- status
- requirement
- 현재 level
- special rule
- 목표 버튼
- 완료 버튼

등이 쌓인다.

게임 도중에는 너무 많이 읽어야 한다.

오른쪽은 **경로 시각화**로 바꾼다.

---

# 5. 새 Tablet Landscape 기본 화면

목표 레이아웃:

```text
┌──────────────────────────────────────────────────────────────┐
│ MS Companion       완료 0/3       ↶ Undo       새 Run   ⋯   │
├────────────────────────────────┬─────────────────────────────┤
│                                │ TARGET                      │
│  MAGIC                         │ A 데몬 방정식               │
│                                │ 화염구 5/7 → 대폭발          │
│ [화염구 5 A +][낙뢰 2  +] ...  │ 에너지탄 2/7 → 플레어        │
│ [운석 1   +][정령 3  +] ...    │                             │
│ [ ... 21개를 고정 위치 ... ]   │ NEED NOW                    │
│                                │ 에너지탄 → 플레어      A     │
│ PASSIVE                        │ 낙뢰 → 청천벽력         B     │
│ [지능 2 +][집중 1 +][발산 0 +] │                             │
│                                │ FIREBALL PATHS              │
│                                │ 대폭발                      │
│                                │  데몬 방정식                │
│                                │  + 에너지탄 → 플레어         │
│                                │                             │
│                                │ 독가스                      │
│                                │  겨울 폭풍 + 눈보라→혹한기   │
└────────────────────────────────┴─────────────────────────────┘
```

왼쪽:

```text
약 58~62%
```

오른쪽:

```text
약 38~42%
```

1024x768에서 전체 body가 스크롤되지 않는 것을 목표로 한다.

---

# 6. 오른쪽 패널의 정보 구조

오른쪽은 3영역이면 충분하다.

## 6.1 TARGET

현재 pin한 조합.

최대 기본 3개.

예:

```text
A 데몬 방정식
✓ 화염구 MAX → 대폭발
○ 에너지탄 4/7 → 플레어
```

목표를 완료하면:

```text
A 데몬 방정식 ✓
```

로 매우 compact하게 축소 가능.

---

# 6.2 NEED NOW

현재 목표를 완성하기 위해
아직 필요한 마법/특성을 자동으로 뽑아낸 목록.

이것은 게임의 랜덤 3~4택 구조 때문에 매우 중요하다.

예:

```text
필요
A 에너지탄 4/7 → 플레어
B 낙뢰 2/7 → 청천벽력
B 전기충격 0/7 → 연쇄 번개
```

이미 MAX + 올바른 특성:

```text
✓
```

잘못된 특성:

```text
× 경로 막힘
```

로 표시.

사용자는 게임의 선택지 3개와 이 목록만 빠르게 대조하면 된다.

---

# 6.3 FOCUSED MAGIC PATH

왼쪽에서 특정 마법을 탭해 살펴볼 때만 표시.

예:

```text
화염구

대폭발
A 데몬 방정식
+ 에너지탄 → 플레어

독가스
겨울 폭풍
+ 눈보라 → 혹한기

흑사병
+ 마력탄 Lv.7 → 난사

별자리
백귀야행
+ 위성 → 도깨비불

원점폭발
+ 에너지탄 → 플레어
```

기본적으로 BLOCKED 경로는 숨긴다.

---

# 7. Trait Picker도 게임 목적에 맞게 수정

현재 긴 effect summary를 기본으로 보여주지 마라.

특성 선택에서 가장 중요한 정보:

```text
이 특성을 선택하면 어떤 조합으로 갈 수 있는가?
```

예:

```text
화염구 Lv.7

┌ 대폭발 ──────────────┐
│ A 데몬 방정식        │
│ + 에너지탄 → 플레어  │
└─────────────────────┘

┌ 독가스 ──────────────┐
│ 겨울 폭풍            │
│ 흑사병               │
└─────────────────────┘

┌ 별자리 ──────────────┐
│ 백귀야행             │
│ 원점폭발             │
│ 제네시스             │
└─────────────────────┘
```

긴 효과 설명은:

```text
ⓘ 상세
```

또는 작은 secondary 영역으로 내린다.

---

# 8. 목표 조합 선택 UX

전체 Catalog는 기본 화면이 아니다.

Focused Magic Path에서 조합명을 누르면 즉시 pin/unpin 가능하게 한다.

예:

```text
대폭발
[☆ 데몬 방정식]
+ 에너지탄 → 플레어
```

누르면:

```text
[A 데몬 방정식]
```

이 되고
해당 두 마법 Tile에도 A badge가 붙는다.

추가 `목표 지정` 버튼을 별도로 크게 만들 필요 없다.

---

# 9. 목표 Badge의 역할

목표 A:

```text
A 데몬 방정식
```

이면:

```text
화염구 Tile → A
에너지탄 Tile → A
```

가 표시된다.

목표 B가 같은 마법을 사용한다면:

```text
A B
```

처럼 둘 다 표시.

그러나 이 경우 실제 동시 달성이 불가능하다면
즉시 충돌 경고를 표시한다.

---

# 10. 목표 충돌

충돌 없을 때는 아무 메시지도 보여주지 않는다.

충돌이 있을 때만:

```text
⚠ A · B 충돌

둘 다 에너지탄을 사용합니다.
동시에 완성할 수 없습니다.
```

같이 크게 표시.

또는 동일한 마법의 다른 특성 요구:

```text
⚠ 화염구 경로 충돌

A → 대폭발
B → 별자리
```

---

# 11. 완료된 조합 처리

조합 조건이 모두 충족되면 Target 영역에서:

```text
A 데몬 방정식
✓ 준비 완료        [완료]
```

정도로 보여라.

완료를 누르면 existing engine을 사용해:

- active magic lock
- 조합 슬롯 사용
- conflict 재계산
- 다른 불가능 경로 제거

를 즉시 수행한다.

---

# 12. 이미 불가능해진 정보는 기본 화면에서 제거

게임 도중 사용자는:

```text
"지금 무엇이 가능한가"
```

가 궁금하다.

따라서:

- 이미 소비된 마법 필요
- 잘못된 특성 선택
- slot 부족
- special rule 불충족

등으로 막힌 조합은
Focused Path에서 기본적으로 보여주지 않는다.

필요하면 아주 작은:

```text
불가능 5
```

버튼으로 확인.

---

# 13. Magic Tile 세부 설계

추천 형태:

```text
┌─────────────┐
│ 화염구    A │
│ 4 / 7   [+] │
└─────────────┘
```

MAX:

```text
┌─────────────┐
│ 화염구    A │
│ 대폭발      │
└─────────────┘
```

조합에 사용됨:

```text
┌─────────────┐
│ 화염구    A │
│ ✓ 조합됨    │
└─────────────┘
```

### 상호작용

카드 본체:

```text
focus only
```

오른쪽의 작은/명확한 + hit area:

```text
level +1
+
focus
```

`+` touch target은 44px 이상.

마법을 실제 게임에서 골랐을 때
`+` 한 번이면 기록 완료.

---

# 14. Grid는 절대 재정렬하지 마라

보유 여부,
목표 여부,
레벨,
검색 여부로
기본 21개 Grid의 위치를 바꾸지 않는다.

고정된 canonical order를 사용한다.

seed JSON 순서를 기준으로 시작해도 된다.

이것은 **게임 도중 시선 탐색 속도**를 위해 의도적인 요구사항이다.

---

# 15. Passive Row

조합에 관련된 현재 3개 패시브는
별도 큰 카드가 필요 없다.

```text
[지능 2/5 +]
[집중 1/3 +]
[마력 발산 0/3 +]
```

한 줄.

패시브가 조합 Target에 필요하면
A/B badge 표시 가능.

---

# 16. Header

높이:

```text
48~54px
```

내용:

```text
MS Companion
완료 0/3
Undo
새 Run
⋯
```

`⋯` 안으로 이동할 것:

- 저장한 빌드
- 전체 조합 Catalog
- 데이터 Audit
- 보너스 조합 슬롯 / Artifact toggle
- 기타 설정

게임 중 기본 화면에는 노출하지 않는다.

---

# 17. 상세 기능은 "Game Mode" 밖으로

현재 존재하는:

- Search
- 전체 Catalog
- Saved Builds 관리
- Audit
- 상세 설명

은 삭제하지 않아도 된다.

그러나 **Game Mode의 visual hierarchy에서 제거**한다.

필요하면:

```text
/
```

= Game Mode

```text
/explore
```

= 전체 조합 탐색

```text
/audit
```

= 데이터 검수

같이 분리해도 된다.

앱 실행 기본은 무조건 Game Mode.

---

# 18. saved build를 실제 플레이에 맞게 사용

saved build를 불러오면:

```text
A / B / C 목표 조합
```

만 pin한다.

마법 레벨은 0부터.

그러면 Run 시작 직후 NEED NOW가 바로 생성된다.

예:

```text
A 데몬 방정식
B 번개 작렬
C 블랙홀

NEED NOW

화염구 → 대폭발
에너지탄 → 플레어
낙뢰 → 청천벽력
전기충격 → 연쇄 번개
돌개바람 → 보이드
위성 → 핵융합
```

이것이 TFT의 덱 가이드처럼 사용하는 핵심 경험이다.

---

# 19. 사용자가 아직 목표를 안 정했을 때

목표가 없어도 앱이 빈 화면처럼 보여서는 안 된다.

마법을 focus하면 바로 경로를 보여준다.

예:

```text
목표 조합 없음

마법을 눌러 조합 경로를 확인하세요.
```

그리고 화염구 focus 즉시:

```text
화염구
대폭발 → ...
독가스 → ...
별자리 → ...
```

---

# 20. 게임 선택지를 앱에 매번 3개 입력시키지 마라

게임이 3~4개의 랜덤 선택지를 보여준다는 사실 때문에
Companion에:

```text
현재 게임에 뜬 3개를 모두 입력하세요
```

같은 기능을 기본 흐름으로 만들지 마라.

그건 매 레벨마다 3번씩 추가 터치해야 해서
Companion 자체가 일이 된다.

대신:

```text
고정 Grid
+
NEED NOW
```

를 빠르게 눈으로 대조하게 한다.

플레이어가 실제로 선택한 마법 하나만
`+`로 기록하면 된다.

---

# 21. "추천"과 "암기 보조"를 구분

앱은 AI 빌드 추천기가 아니다.

나쁜 예:

```text
지금 화염구를 고르는 것이 최적입니다.
```

좋은 예:

```text
화염구
A 데몬 방정식 재료
현재 5/7 → 대폭발 필요
```

사용자가 전략을 선택한다.

---

# 22. 정보량 규칙

Game Mode에서 기본적으로 숨길 것:

- 긴 마법 설명
- 피해량 수치 설명
- Artifact 설명
- Wiki 출처
- verification 상태
- 개발자용 status 명칭
- special rule 긴 문장
- 설명성 paragraph
- decorative intro

필요할 때만 펼친다.

---

# 23. 상태 표현

엔진 내부의:

```text
READY
IN_PROGRESS
AVAILABLE
BLOCKED
COMPLETED
```

는 유지.

Game Mode에서는:

```text
✓
○
×
```

와 간단한 한국어만.

예:

```text
✓ 준비
○ 필요
× 불가
```

---

# 24. 시각 디자인

현재 dark theme는 유지해도 좋다.

다만 **card inside card inside card** 느낌을 제거한다.

권장:

- 어두운 단색 배경
- 얇은 separator
- compact tile
- Target badge만 accent
- border 최소화
- 그림자 최소화
- decorative icon 최소화

이 앱은 게임보다 시선을 더 빼앗으면 안 된다.

---

# 25. 1024x768 Acceptance Criteria

이 화면에서 반드시 직접 QA한다.

```text
1024 x 768
```

다음이 동시에 보여야 한다.

- compact header
- 액티브 마법 21개 전체
- 패시브 3개
- 목표 조합
- NEED NOW
- Focused magic path의 핵심 일부
- Undo
- New Run

**전체 body 세로 스크롤이 없어야 한다.**

오른쪽 path가 너무 길면
오른쪽 panel 내부만 필요할 때 제한적으로 스크롤 가능하지만,
Target과 NEED NOW는 sticky/fixed 영역으로 계속 보여야 한다.

---

# 26. Tablet Portrait

```text
768 x 1024
```

에서는:

- 가능하면 2 column
- magic grid의 타일은 여전히 고정 위치
- 오른쪽 폭이 지나치게 좁으면
  아래 panel / drawer 구조 허용

하지만 landscape를 희생하지 않는다.

---

# 27. Mobile

```text
390 x 844
```

에서는 기존 bottom sheet 전략을 사용할 수 있다.

예:

```text
Magic Grid
[목표/조합 보기]
```

단 Mobile은 3순위다.

Tablet landscape의 밀도와 속도가 최우선.

---

# 28. PWA / Offline / LocalStorage

기존 기능 유지.

게임 도중 네트워크 요청 없이 동작.

Run 상태 자동 저장.

새로고침/브라우저 재실행 후 복원.

---

# 29. 현재 엔진/데이터는 최대한 보존

다시 만들지 말아야 할 것:

- `evaluateCombination`
- `getRelevantCombinations`
- `getCombinationConflicts`
- `getMagicPaths`
- `completeCombination`
- `locks`
- `applyEffects`
- `validateGameData`
- storage serialization

기존 테스트가 정상이라면 엔진을 갈아엎지 않는다.

이번 작업은 UI/interaction을 제품 목적에 맞게 재구성하는 것이 핵심이다.

---

# 30. main.tsx 분리

현재 `main.tsx`가 너무 크다면
이번 refactor에서 다음 정도로 분리해도 된다.

```text
src/features/game-mode/
  GameHeader.tsx
  MagicGrid.tsx
  MagicTile.tsx
  PassiveRow.tsx
  TargetPanel.tsx
  NeedNow.tsx
  MagicPathPanel.tsx
  TraitPicker.tsx
```

하지만 refactor 자체가 목적이 아니다.

사용성 개선이 우선이다.

---

# 31. 반드시 추가할 derived selector

Target을 기반으로
`NEED NOW`를 계산하는 pure function을 만든다.

예:

```ts
getNeededMagicForTargets(run)
```

반환 정보 예:

```ts
{
  magicId: "energy_bolt",
  currentLevel: 4,
  maxLevel: 7,
  requiredTraitId: "flare",
  targetCombinationIds: ["demon_equation"],
  state: "leveling"
}
```

가능 상태:

```text
not_owned
leveling
trait_needed
ready
blocked
consumed
```

마력탄 multi-stage trait도 정확하게 처리.

---

# 32. Focused Magic Path selector

기존 `getMagicPaths`를 활용/보완해서:

```text
특성
→ 아직 가능한 조합
→ 상대 재료
→ 현재 보유 상태
→ Target badge
```

를 바로 반환하도록 한다.

UI 안에서 복잡한 계산을 반복하지 않는다.

---

# 33. E2E — 실제 게임 상황을 시뮬레이션

기존 테스트뿐 아니라 다음 UX 시나리오를 검증한다.

## Scenario A — Run 시작

saved build:

```text
A 데몬 방정식
B 번개 작렬
```

불러오기.

첫 화면에서 즉시:

```text
화염구 → 대폭발
에너지탄 → 플레어
낙뢰 → 청천벽력
전기충격 → 연쇄 번개
```

가 NEED NOW에 보여야 한다.

검색/스크롤 불필요.

---

## Scenario B — 게임 레벨업에서 화염구가 뜸

사용자는 Magic Grid에서 화염구를 바로 찾는다.

`+` 한 번.

결과:

```text
화염구 Lv.1
A badge
```

즉시 갱신.

---

## Scenario C — 다음 선택 전에 경로 확인

사용자가 낙뢰 Tile 본체를 탭.

레벨은 변하지 않는다.

오른쪽에서:

```text
청천벽력
→ 번개 작렬
+ 전기충격 / 연쇄 번개
```

등이 즉시 보인다.

---

## Scenario D — 잘못된 전직

A가 화염구/대폭발을 요구하는데
화염구 Lv.7에서 독가스를 선택.

짧은 경고:

```text
A 데몬 방정식이 막힙니다.
```

---

## Scenario E — 조합 완성

화염구/대폭발 + 에너지탄/플레어 완료.

Target:

```text
A 데몬 방정식
✓ 준비 완료
```

완료 처리.

이후 두 마법을 요구하는 다른 조합이
Game Mode 경로에서 기본적으로 사라진다.

---

## Scenario F — 3초 glance test

1024x768에서 사용자가:

```text
게임 화면:
에너지탄 / 서릿발 / 집중
```

을 보고 있다고 가정.

Companion을 봤을 때
3초 안에:

```text
에너지탄 = Target A 필요
```

를 알 수 있어야 한다.

이것이 가장 중요한 Acceptance Test다.

---

# 34. 성능

모든 계산 local.

사용자 터치 후 UI feedback:

```text
즉시
```

느껴져야 한다.

불필요한 animation 제거.

transition은 최대 100~150ms 정도.

---

# 35. 하지 말 것

이번 작업에서 하지 마라.

- Artifact 전체 시스템 추가
- Ultimate 전체 시스템 추가
- AI 추천
- 티어 추천
- DPS 계산
- 커뮤니티 기능
- 계정
- 서버
- 게임 화면 OCR
- 게임 선택지 3개를 매번 입력하는 UX
- 마법 grid를 필터에 따라 재정렬
- 설명문 확대
- 새 대시보드 추가

---

# 36. 작업 순서

1. 현재 `main.tsx`, `style.css`, engine, storage를 읽어라.
2. 기존 테스트를 먼저 실행해 baseline을 확인하라.
3. engine/data는 그대로 두고 Game Mode의 정보 구조를 재설계하라.
4. `getNeededMagicForTargets` 같은 derived selector를 추가하라.
5. 1024x768 compact grid부터 구현하라.
6. Target / NEED NOW / Focused Path panel을 구현하라.
7. inspect와 level record 동작을 분리하라.
8. Trait Picker를 조합 경로 중심으로 압축하라.
9. search/filter/catalog/audit/saved-build 관리는 secondary UI로 이동하라.
10. responsive 수정.
11. 기존 unit tests + 신규 tests.
12. Playwright/e2e 가능하면 실행.
13. `npm run test`
14. `npm run build`
15. 최종적으로 1024x768에서 스크롤/overflow를 확인하라.

---

# 37. 완료 보고에 포함할 것

완료 후 다음을 짧게 보고한다.

```text
- 어떤 UI를 제거/축소했는지
- 1024x768에서 21개 마법이 모두 보이는지
- NEED NOW가 어떻게 계산되는지
- inspect와 level 기록 UX가 어떻게 분리됐는지
- 목표 충돌/완료 후 lock이 정상인지
- test/build 결과
```

---

# 38. 최종 제품 판단 기준

이 앱은 예쁜 사이트여서 성공하는 것이 아니다.

다음 순간에 성공해야 한다.

```text
게임에서 3개의 마법 선택지가 떴다.
↓
태블릿을 본다.
↓
내 Target에 필요한 것이 무엇인지 바로 보인다.
↓
필요하면 마법 하나를 눌러 조합 경로를 확인한다.
↓
게임에서 선택한다.
↓
Companion의 +를 한 번 눌러 기록한다.
↓
게임으로 돌아간다.
```

이 흐름에 검색, 긴 설명, 전체 조합표 탐색, 여러 번의 스크롤이 끼어들지 않아야 한다.

---

# 39. 조사 근거 — Codex가 알아야 할 게임 맥락 출처

아래 자료를 기준으로 위 플레이 구조를 정리했다.

## 공식 Google Play

Magic Survival 현재 v0.992.
v0.99에서 신규 Combination Magic, Ultimate Magic, Artifact, Synergy 등이 추가됨.

```text
https://play.google.com/store/apps/details?id=com.vkslrzm.Zombie
```

## 일본 Magic Survival 공략 Wiki — 기본 시스템

레벨업 시 랜덤 3택,
기존에 습득한 마법의 재등장 경향,
일부 효과에 의한 4택,
Mana Retrieval,
Lv.25/50/75의 조합 가능 횟수 증가와
레벨업 화면 상단의 조합 버튼 등을 확인한 자료.

```text
https://w.atwiki.jp/magicsurvival/pages/53.html
```

## 일본 Magic Survival 공략 Wiki — 마법 / 조합

전투 마법/패시브 구조,
최대 레벨 특성 선택,
조합의 올바른 특성 요구,
강화 주체와 재료의 구분,
마력탄의 다단계 특성 예외,
기본 3회의 조합 등을 확인한 자료.

```text
https://w.atwiki.jp/magicsurvival/pages/15.html
```

## 일본 Magic Survival 공략 Wiki — FAQ

Lv.100 Ultimate Magic의 별도 선택 흐름,
기본 최대 레벨 100,
특수 조합 예외 등을 확인한 자료.

```text
https://w.atwiki.jp/magicsurvival/pages/5.html
```

## 일본 Magic Survival 공략 Wiki — Artifact

보물상자에서 랜덤 Artifact 3택,
웨이브/상인 등 Artifact의 별도 선택 흐름을 확인한 자료.

```text
https://w.atwiki.jp/magicsurvival/pages/34.html
```

이 출처들은 디자인 장식용 참고가 아니라
**왜 Companion의 UX가 이렇게 되어야 하는지 이해하기 위한 게임 규칙 근거**다.

---

# 40. 바로 수정 작업을 시작하라

현재 앱의 기능적 기반은 재사용한다.

이번 작업의 목표는:

> **공략 사이트처럼 읽는 UI → 게임을 하면서 흘끗 보고 바로 판단하는 HUD**

로 바꾸는 것이다.

새 기능을 과하게 추가하지 말고,
실제 Magic Survival의 레벨업/특성/조합 선택 흐름에 맞춰
Game Mode를 전면 개편하라.
